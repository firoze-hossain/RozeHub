<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $project->name }} · RozeHub</title><link rel="stylesheet" href="{{ asset('css/rozehub.css') }}"></head><body><main style="max-width:1100px;margin:40px auto;padding:20px"><a href="{{ route('ecosystem.index') }}">← Ecosystem</a><h1>{{ $project->name }}</h1><p>{{ $project->tagline }}</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:14px"><article><h3>Health</h3><strong style="font-size:32px">{{ $health['score'] }}%</strong></article>@foreach($health['breakdown'] as $key=>$value)<article><h3>{{ str_replace('_',' ',ucwords($key)) }}</h3><strong>{{ $value }}%</strong></article>@endforeach</div><h2>Roadmap</h2>@forelse($roadmaps as $roadmap)<section style="border:1px solid #ddd;border-radius:14px;padding:18px;margin:12px 0"><h3>{{ $roadmap->title }}</h3><p>{{ $roadmap->description }}</p><ul>@foreach($roadmap->items as $item)<li><b>{{ $item->title }}</b> — {{ str_replace('_',' ', $item->status) }} @if($item->target_version) · {{ $item->target_version }} @endif</li>@endforeach</ul></section>@empty<p>No roadmap published yet.</p>@endforelse<h2>Ecosystem graph</h2><pre style="overflow:auto;background:#f6f6f6;padding:16px;border-radius:12px">{{ json_encode($graph,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></main></body></html>
