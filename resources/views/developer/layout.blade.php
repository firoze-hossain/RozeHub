<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $title ?? 'Developer Center · RozeHub' }}</title><link rel="icon" href="{{ asset('images/rozehub-icon.png') }}"><link rel="stylesheet" href="{{ asset('css/rozehub.css') }}"><link rel="stylesheet" href="{{ asset('css/developer-marketplace.css') }}"></head><body class="developer-body"><header class="dev-top"><a href="{{ route('hub') }}" class="dev-brand"><span>R</span><strong>RozeHub</strong><small>DEVELOPER CENTER</small></a><nav><a href="{{ route('developer.dashboard') }}">Dashboard</a><a href="{{ route('developer.marketplace.submissions') }}">Submissions</a><a href="{{ route('marketplace.index') }}">Marketplace</a><a href="{{ route('developer.notifications') }}">Notifications @if(($unread ?? 0)>0)<b>{{ $unread }}</b>@endif</a></nav><div class="dev-account">{{ auth()->user()->name }}<form method="POST" action="{{ route('developer.logout') }}">@csrf<button>Sign out</button></form></div></header><main class="dev-main">@if(session('success'))<div class="dev-alert success">{{ session('success') }}</div>@endif@if($errors->any())<div class="dev-alert error"><strong>Please fix:</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif@yield('content')</main></body></html>
