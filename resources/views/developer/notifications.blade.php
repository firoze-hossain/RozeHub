@extends('developer.layout',['title'=>'Notifications · RozeHub'])
@section('content')<div class="dev-page-head"><div><span class="eyebrow">DEVELOPER NOTIFICATIONS</span><h1>Marketplace activity</h1></div></div><div class="notification-list">@forelse($notifications as $n)<article class="notification {{ $n->read_at?'':'unread' }}"><div><span class="eyebrow">{{ strtoupper($n->type) }}</span><h3>{{ $n->title }}</h3><p>{{ $n->message }}</p><small>{{ $n->created_at->diffForHumans() }}</small></div><div>@if($n->action_url)<a href="{{ $n->action_url }}">Open</a>@endif @if(!$n->read_at)<form method="POST" action="{{ route('developer.notifications.read',$n) }}">@csrf @method('PATCH')<button>Mark read</button></form>@endif</div></article>@empty<div class="dev-empty">No notifications.</div>@endforelse</div>{{ $notifications->links() }}@endsection
