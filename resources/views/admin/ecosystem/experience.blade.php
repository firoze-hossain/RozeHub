@extends('admin.layout')
@section('content')
<div class="admin-card"><div style="display:flex;justify-content:space-between;align-items:center"><div><h2>Phase 6 · Ecosystem Experience</h2><p>Manage the dynamic community, health and roadmap layer.</p></div><form method="POST" action="{{ route('admin.ecosystem-experience.graph.sync') }}">@csrf<button class="admin-btn" type="submit">Sync graph</button></form></div></div>
<div class="admin-grid" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:18px">@foreach($projects as $project)<article class="admin-card"><h3>{{ $project->name }}</h3><p>{{ $project->tagline }}</p><div style="font-size:28px;font-weight:700">{{ $healthData[$project->id]['score'] }}%</div><small>Health score</small><form method="POST" action="{{ route('admin.ecosystem-experience.metrics.save',$project) }}" style="margin-top:18px">@csrf<input type="hidden" name="metric_key" value="community_activity"><label>Community weight</label><input name="weight" type="number" step="0.1" value="1"><label>Override score</label><input name="score" type="number" min="0" max="100" value="{{ $healthData[$project->id]['breakdown']['community_activity'] }}"><button class="admin-btn" type="submit">Save metric</button></form><hr><form method="POST" action="{{ route('admin.ecosystem-experience.roadmaps.save',$project) }}">@csrf<input name="title" placeholder="New roadmap title" required><textarea name="description" placeholder="Description"></textarea><select name="status"><option value="active">Active</option><option value="planned">Planned</option><option value="archived">Archived</option></select><button class="admin-btn" type="submit">Create roadmap</button></form></article>@endforeach</div>
@endsection
