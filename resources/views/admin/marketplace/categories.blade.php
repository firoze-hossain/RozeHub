@extends('admin.layout')
@section('content')<div class="admin-page-head"><h1>Marketplace categories</h1><p>Define categories independently for each project ecosystem.</p></div><form method="POST" action="{{ route('admin.marketplace.categories.store') }}">@csrf<select name="software_project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->name }}</option>@endforeach</select><input name="name" placeholder="Category name" required><input name="description" placeholder="Description"><input name="icon" placeholder="Icon"><button type="submit">Add category</button></form>@foreach($projects as $p)<h2>{{ $p->name }}</h2>@foreach($p->marketplaceCategories as $c)<form method="POST" action="{{ route('admin.marketplace.categories.update',$c) }}" style="display:flex;gap:8px;margin:8px 0">@csrf @method('PUT')<input name="name" value="{{ $c->name }}"><input name="description" value="{{ $c->description }}"><input type="number" name="sort_order" value="{{ $c->sort_order }}"><button>Save</button></form>@endforeach @endforeach@endsection
