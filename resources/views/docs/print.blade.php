<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $project->name }} Documentation{{ $selectedRelease ? ' · v'.$selectedRelease->version : '' }} · PDF</title><link rel="stylesheet" href="{{ asset('css/rozehub.css') }}"></head><body class="docs-print-body"><div class="docs-print-toolbar"><strong>{{ $project->name }} Documentation{{ $selectedRelease ? ' · v'.$selectedRelease->version : '' }}</strong><span class="docs-print-hint">Use your browser’s Print → Save as PDF</span><a href="{{ route('docs.project',$project) }}{{ $selectedRelease ? '?release='.$selectedRelease->id : '' }}">Back to docs</a></div><header class="docs-print-cover"><span>ROZEHUB DOCUMENTATION</span><h1>{{ $project->name }}</h1>@if($selectedRelease)<div class="docs-print-version">Version {{ $selectedRelease->version }} @if($selectedRelease->codename) · {{ $selectedRelease->codename }}@endif</div>@endif<p>{{ $project->description }}</p><small>Generated {{ now()->format('d M Y H:i') }}</small></header>@foreach($project->documentationSections as $section)<section class="docs-print-section"><h2>{{ $section->title }}</h2>@if($section->description)<p class="docs-print-section-desc">{{ $section->description }}</p>@endif@foreach($section->pages as $page)<article class="docs-print-page"><span class="docs-kind">{{ strtoupper(str_replace('-',' ',$page->kind)) }} · {{ $page->release ? 'v'.$page->release->version : 'All versions' }}</span><h3>{{ $page->title }}</h3>@if($page->summary)<p class="docs-print-summary">{{ $page->summary }}</p>@endif<div class="docs-content">{!! $page->rendered_html !!}</div></article>@endforeach</section>@endforeach</body></html>
